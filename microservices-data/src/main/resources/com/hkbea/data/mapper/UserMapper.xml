<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkbea.data.mapper.UserMapper">

	<resultMap type="com.hkbea.data.model.SysUser" id="userMap">
		<id property="id" column="id" />
		<result property="userName" column="user_name" />
		<result property="userPassword" column="user_password" />
		<result property="userEmail" column="user_email" />
		<result property="userInfo" column="user_info" />
		<result property="headImg" column="head_img" jdbcType="BLOB" />
		<result property="createTime" column="create_time" jdbcType="TIMESTAMP" />
	</resultMap>

	<select id="selectById" parameterType="long" resultMap="userMap">
		SELECT
			id, 
			user_name, 
			user_password, 
			user_email, 
			user_info, 
			head_img,
			create_time 
		FROM sys_user 
		WHERE id = #{id}
	</select>

	<select id="selectAll" resultType="com.hkbea.data.model.SysUser">
		SELECT 
			id, 
			user_name as userName, 
			user_password as userPassword, 
			user_email as userEmail, 
			user_info as userInfo, 
			head_img as headImg, 
			create_time as createTime 
		FROM sys_user 
		WHERE 1 = 1
	</select>
	
	<select id="selectRolesByUserId" parameterType="long" resultType="com.hkbea.data.model.SysRole">
		SELECT 
			r.id,
			r.role_name as roleName,
			r.enabled,
			r.create_by as createBy,
			r.create_time as createTime
		FROM
			sys_role r left join sys_user_role ur on ur.role_id = r.id 
			left join sys_user u on ur.user_id = u.id
			WHERE u.id = #{id} 
	</select>
	
	<insert id="insert" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO sys_user(
			user_name, 
			user_password, 
			user_email, 
			user_info, 
			head_img, 
			create_time)
		VALUES(
			#{userName},
			#{userPassword},
			#{userEmail},
			#{userInfo},
			#{headImg, jdbcType=BLOB },
			#{createTime, jdbcType=TIMESTAMP}
		)
	</insert>
	
	<insert id="insert3">
		INSERT INTO sys_user(
			user_name, 
			user_password, 
			user_email, 
			user_info, 
			head_img, 
			create_time)
		VALUES(
			#{userName},
			#{userPassword},
			#{userEmail},
			#{userInfo},
			#{headImg},
			now()
		)
		<selectKey keyProperty="id" keyColumn="id" resultType="long" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
</mapper>